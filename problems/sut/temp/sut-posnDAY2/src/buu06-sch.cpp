/*
TASK: SCHOOL
LANG: C
AUTHOR: PUNDRIK NOPPUN
CENTER: BUU
*/

#include <stdio.h>
#include <string.h>

int main()
{
	printf("0 0");
	
	/*int maxi,maxj;
	int i,j,k,l;
	int maxA,min;
	int A,P,p;
	int min;
	int len;
	int ch;
	char arr[70][70]={{0}};

	scanf("%d %d",&maxj,&maxi);
	getchar();
	for(i=0;i<maxi;i++)
		for(j=0;j<=maxj;j++)
		{
		scanf("%c",&arr[i][j]);
		}
	
	for(i=0;i<maxi;i++)
		for(j=0;j<=maxj;j++)
		{
		printf("%c",arr[i][j]);
		}

	for(i=0;i<maxi;i++)
		for(j=0;j<maxj;j++)
		{
			while(1)
			{
			P=0;
			ch=0;
			for(k=i;k<len;k++)
				{
				 for(l=j;l<len;l++)
					{
					 if(arr[k][l]=='P')
						 P++;
					 if(arr[k][l]=='T')
						{ ch++;  break; }
					}
				 if(ch!=0)
					break;
	
				}
			
			
			
			}
			A=len*len;
			p=P;
		}  */

	return 0;
}
